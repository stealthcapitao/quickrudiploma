<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
      <title>Quickru</title>
      <link rel="stylesheet" href="/stylesheets/main.css">
      
  </head>
  


<body>
  <!-- Header -->
  <div id="header-wrapper">
    <div id="header" class="container">
      <div id="logo">
        <h1><span class="<%= typeof page !== 'undefined' && page === 'orders' ? 'active' : '' %>"></span><a href="/orders">Quickru</a></h1>
      </div>

      <!-- Menu -->
      <div id="menu">
        <ul>
  <div class="container">
    <div class="navbar-header">

    <!-- Header mobile button -->
      <button type="button" style="display: none;">

        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

            <!-- Hide navbar -->
                      <li><a class="link" href="/Customer">For Customers</a></li>
                      <li><a class="link" href="/Aboutus">About Us</a></li>
          <% if(!currentUser){ %>
                      <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login">Login</a></li>
                      <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register">Sign Up</a></li>
            <% } else { %>
              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
                      <li style="color: #c29e2a; ">Hello:<a href="/users/<%= currentUser.id %>" style="padding: 0px 10px;"><%= currentUser.username %></a><a style="color: red; background-color: rgba(255, 0, 0, 0.272);
                        border-radius: 5%; padding: 10px;" href="/logout">Logout</a></li>
                      
              <% } %>
        </ul>


      </div>
    </div>
  </div>
  
  </nav>

  <!-- Header block -->
  
  <div id="header-block"></div><!-- !!!!!!!!!изменить пустой блок под хэдэр-->
  
  <!-- Header success -->
  
  <div class="container" style=" text-align: center; background-color: white; ">
    <% if(error && error.length > 0) { %>

              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
              <!-- FIX THAT!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
      <div class="alert alert-danger" style="color:lightpink"><p style="color:red"><%=error %></p></div>
    <% } %>
    
    <% if(success && success.length > 0) { %>
      <div class="alert alert-success" style="color:lightgreen"><p style="color:green"><%= success %></p></div>
    <% } %>
   </div>
   <div class="container">
